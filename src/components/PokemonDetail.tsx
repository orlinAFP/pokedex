import React from 'react'
import { ScrollView, StyleSheet, Text } from 'react-native'

export const PokemonDetail = () => {
  return (
    <ScrollView style={{backgroundColor:'red', ...StyleSheet.absoluteFillObject}}>
        <Text>Hello</Text>
    </ScrollView>
  )
}
